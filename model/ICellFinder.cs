namespace model
{
    //public interface ICellFinder
    //{
    //    Cell Pick(Cell[,] field, int row, int column, out int newRow, out int newColumn);
    //    void PickWithoutAgent(Cell[,] field, int row, int column, out int newRow, out int newColumn);
    //}
}